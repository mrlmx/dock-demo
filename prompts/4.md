1.

```md
刚才运行的时候，我发现了 2 个问题：

1. Dock 在右侧吸附的会漏出一个边框，这是为了让用户知道这里有个 Dock 吗？咱们能不能支持自定义，用户可以选择是否漏出（最好是那种可以自定义漏出多少距离的配置），默认为 0 ，一点都不漏出，另外这个配置，需要支持Dock 在 4 个方位的场景（也就是同这 4 个方位共享这一个漏出距离的配置）。

2. Dock 在右侧吸附时，并没有垂直居中，你检查一下是什么原因。
```